# Selenium
# selenium automate browsers
# Selenium - 3 - 20% ppl are using Selenium 3, json wire protocol
# Selenium 4 - 70% + are using Selenium 4 - w3c protocol, selenium manager

# if we have pip install selenium -> 4.x > yo dont have to set up the browser drivers

# In selenium 3 we used to import webdriver like below
# from selenium import webdriver
# def
#driver_path = '' # path of thr driver
# driver = webdriver.EdgeService(executable_path = driver_path)
# driver.get("https://app.vwo.com")
